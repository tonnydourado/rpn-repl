 * Add tests;
    * How to test the repl? Just calling the methods?
    * testing failure conditions
 * Implementing variable arity using parenthesys, each () block starts a new stack;
 * REPL must read from custom file, defaulting to sys.stdin;